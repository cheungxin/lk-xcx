# 微信小程序基座模板 (Template Base)

基于 Skyline 渲染引擎的现代化微信小程序开发模板，提供完整的开发基础设施。

## 特性

- ✅ **Skyline 渲染引擎** - 使用微信最新的 Skyline 渲染引擎，提供更好的性能
- ✅ **模块化架构** - 清晰的目录结构，便于代码组织和维护
- ✅ **统一路由管理** - 集中式路由配置和跳转封装
- ✅ **网络请求封装** - 统一的 HTTP 请求处理，支持拦截器
- ✅ **环境配置管理** - 支持开发/生产环境切换
- ✅ **工具函数库** - 提供常用的工具函数
- ✅ **云开发支持** - 可选的云开发能力
- ✅ **自定义导航栏** - 适配 Skyline 的自定义导航栏组件
- ✅ **分包加载** - 支持普通分包和独立分包
- ✅ **错误处理** - 完善的错误处理和日志系统
- ✅ **TDesign 准备** - 为 TDesign 组件库集成预留配置

## 目录结构

```
template-base/
├── miniprogram/                 # 小程序源码目录
│   ├── router/                  # 路由管理
│   │   └── index.js            # 路由管理器
│   ├── config/                  # 配置文件
│   │   └── api-config.js       # API 配置
│   ├── interface/               # 接口层
│   │   ├── request.js          # 网络请求封装
│   │   └── api.js              # API 定义
│   ├── utils/                   # 通用工具
│   │   ├── util.js             # 工具函数
│   │   └── error-handler.js    # 错误处理工具
│   ├── toolbox/                 # 业务工具
│   │   └── tool.js             # 业务工具函数
│   ├── model/                   # 数据模型
│   │   ├── database.js         # 数据库基类
│   │   └── index.js            # 数据模型
│   ├── components/              # 自定义组件
│   │   └── navigation-bar/     # 自定义导航栏
│   ├── pages/                   # 页面
│   │   ├── index/              # 首页
│   │   ├── profile/            # 个人中心
│   │   ├── login/              # 登录页
│   │   ├── list/               # 列表页
│   │   ├── detail/             # 详情页
│   │   └── logs/               # 日志页
│   ├── subpackages/             # 分包
│   │   ├── feature-a/          # 普通分包
│   │   └── feature-b/          # 独立分包
│   ├── assets/                  # 静态资源
│   │   ├── images/             # 图片
│   │   └── icons/              # 图标
│   └── styles/                  # 样式文件
│       └── common.wxss         # 通用样式
├── cloudfunctions/              # 云函数
│   └── example/                # 示例云函数
├── app.js                       # 应用入口
├── app.json                     # 应用配置
├── app.wxss                     # 全局样式
├── project.config.json          # 项目配置
├── sitemap.json                 # 站点地图
├── package.json                 # 依赖管理
├── theme.json                   # 主题配置
└── README.md                    # 项目说明
```

## 快速开始

### 1. 克隆或复制模板

```bash
# 复制 template-base 目录到你的项目位置
cp -r template-base my-miniprogram
cd my-miniprogram
```

### 2. 配置项目

1. 修改 `project.config.json` 中的 `appid` 为你的小程序 AppID
2. 修改 `miniprogram/config/api-config.js` 中的 API 地址
3. 如需使用云开发，配置云开发环境 ID

### 3. 安装依赖（可选）

```bash
npm install
```

### 4. 在微信开发者工具中打开项目

1. 打开微信开发者工具
2. 导入项目，选择 template-base 目录
3. 开始开发

## 核心功能使用

### 路由管理

```javascript
// 引入路由管理器
import router from '../../router/index'

// 页面跳转
router.push('DETAIL', { id: 123 })

// 页面返回
router.back()

// 跳转到首页
router.switchTab('INDEX')
```

### 网络请求

```javascript
// 引入请求模块
const request = require('../../interface/request')

// 发起 GET 请求
const data = await request.get('/api/list', { page: 1 })

// 发起 POST 请求
const result = await request.post('/api/create', { name: 'test' })
```

### 工具函数

```javascript
// 引入工具函数
const util = require('../../utils/util')
const tool = require('../../toolbox/tool')

// 日期格式化
const dateStr = util.formatDate(new Date(), 'YYYY-MM-DD')

// 显示提示
await tool.toast('操作成功')

// 本地存储
await tool.setStorage('key', 'value')
const value = await tool.getStorage('key')
```

### 云函数调用

```javascript
// 引入工具函数
const tool = require('../../toolbox/tool')

// 调用云函数
const result = await tool.callCloud('example', { message: 'hello' })
```

### 数据模型

```javascript
// 引入数据模型
const model = require('../../model/index')

// 注册用户
await model.register({ openid: 'xxx', nickName: '张三' })

// 查询数据
const users = await model.query('users', { age: 18 })

// 更新数据
await model.update('users', 'user-id', { nickName: '李四' })
```

## TDesign 组件库集成

### 1. 安装 TDesign

```bash
npm install tdesign-miniprogram --save
```

### 2. 构建 npm

在微信开发者工具中：
1. 点击菜单栏的 "工具" -> "构建 npm"
2. 等待构建完成

### 3. 全局注册组件

在 `app.json` 中添加组件注册（参考 `tdesign-config.md`）：

```json
{
  "usingComponents": {
    "navigation-bar": "/miniprogram/components/navigation-bar/navigation-bar",
    "t-button": "tdesign-miniprogram/button/button",
    "t-cell": "tdesign-miniprogram/cell/cell",
    "t-icon": "tdesign-miniprogram/icon/icon"
  }
}
```

### 4. 引入样式

在 `app.wxss` 中取消注释 TDesign 样式引入：

```css
@import 'tdesign-miniprogram/miniprogram_dist/common/style/index.wxss';
```

### 5. 使用组件

在页面中直接使用：

```xml
<t-button theme="primary">按钮</t-button>
<t-cell title="单元格" />
```

### 6. 自定义主题

修改 `theme.json` 文件中的主题配置，然后在 `app.wxss` 中应用。

详细文档：https://tdesign.tencent.com/miniprogram/overview

## 分包使用说明

### 普通分包 (feature-a)

普通分包可以使用主包的公共资源：

```javascript
// 在分包页面中使用主包资源
import router from '../../../router/index'
const tool = require('../../../toolbox/tool')
```

### 独立分包 (feature-b)

独立分包不能依赖主包资源，需要独立实现：

```javascript
// 独立分包中直接使用微信 API
wx.navigateTo({ url: '/pages/index/index' })
wx.showToast({ title: '提示' })
```

## 性能优化

### 1. 分包预下载

在 `app.json` 中配置分包预下载规则：

```json
{
  "preloadRule": {
    "pages/index/index": {
      "network": "all",
      "packages": ["feature-a"]
    }
  }
}
```

### 2. 按需注入

已启用 `lazyCodeLoading: "requiredComponents"`，自动按需注入代码。

### 3. 图片优化

- 使用 WebP 格式
- 压缩图片大小
- 使用 CDN 加载

### 4. 代码优化

- 避免在 onLoad 中执行耗时操作
- 使用防抖和节流优化频繁操作
- 合理使用 setData，避免一次性设置大量数据

## 常见问题

### Q: 如何切换开发/生产环境？

A: 修改 `miniprogram/config/api-config.js` 中的 `currentEnv` 变量：

```javascript
let currentEnv = 'development' // 或 'production'
```

### Q: 如何启用云开发？

A: 
1. 在微信开发者工具中开通云开发
2. 在 `miniprogram/config/api-config.js` 中配置云开发环境 ID
3. 在 `app.js` 中初始化云开发环境

### Q: 自定义导航栏高度不正确？

A: 导航栏组件会自动计算高度，如果有问题，检查：
1. `app.json` 中是否设置了 `navigationStyle: "custom"`
2. 页面 json 中是否也设置了 `navigationStyle: "custom"`

### Q: 如何添加新的页面？

A:
1. 在 `miniprogram/pages` 目录下创建页面文件夹
2. 在 `app.json` 的 `pages` 数组中添加页面路径
3. 在 `router/index.js` 的 `ROUTES` 对象中添加路由配置

### Q: 如何使用 TDesign 组件？

A: 参考上面的 "TDesign 组件库集成" 章节。

## 技术栈

- 微信小程序原生开发
- Skyline 渲染引擎
- Glass-Easel 组件框架
- 微信云开发（可选）
- TDesign 组件库（可选）

## 版本要求

- 微信开发者工具：最新版本
- 基础库版本：>= 3.0.0
- Node.js：>= 14.0.0（如需使用 npm）

## 更新日志

### v1.0.0 (2024)
- ✅ 初始版本发布
- ✅ 基于 Skyline 渲染引擎
- ✅ 完整的开发基础设施
- ✅ 示例页面和组件
- ✅ TDesign 集成准备

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 联系方式

如有问题，请通过以下方式联系：
- Issue: 在 GitHub 上提交 Issue
- Email: your-email@example.com

---

**Happy Coding! 🎉**


npm install @amap/amap-jsapi-loader
